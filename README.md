Simple Pure Node.js Web Server

Це простий веб-сервер, написаний на чистому Node.js без використання сторонніх фреймворків (як Express). Сервер вміє віддавати статичні HTML-сторінки, обробляти маршрутизацію та приймати дані з форм через POST-запити.
🚀 Інструкція зі встановлення та запуску

Для роботи проєкту необхідна встановлена платформа Node.js.

    Завантажте проєкт (або перейдіть у папку з файлами).

    Відкрийте термінал у цій папці.

    Запустіть сервер командою:

Bash

npm start

Після успішного запуску ви побачите повідомлення:
Plaintext

Server is running on port 3000
Open http://localhost:3000 in your browser

Тепер ви можете відкрити браузер і перейти за адресою http://localhost:3000.
🌐 Список маршрутів (API Endpoints)

Сервер підтримує наступні маршрути:
1. Головна сторінка

    URL: /

    Метод: GET

    Опис: Повертає головну HTML сторінку (index.html).

    Приклад відповіді: HTML код головної сторінки (200 OK).

2. Про нас

    URL: /about

    Метод: GET

    Опис: Повертає інформаційну сторінку (about.html).

3. Контакти

    URL: /contact

    Метод: GET

    Опис: Повертає сторінку з формою зворотного зв'язку (contact.html).

4. Відправка форми

    URL: /submit

    Метод: POST

    Опис: Приймає дані форми (ім'я та email), санітизує їх та повертає сторінку успіху.

    Вимоги до тіла запиту (Body):

        Тип контенту: application/x-www-form-urlencoded

        Поля: name (обов'язково), email (обов'язково).

    Приклад запиту (curl):
    Bash

    curl -X POST -d "name=User&email=test@example.com" http://localhost:3000/submit

    Приклад успішної відповіді (200 OK): Повертається success.html, де {{name}} та {{email}} замінені на ваші дані.

    Приклад помилки (400 Bad Request): Якщо не передано ім'я або пошту. Текст: 400 Bad Request: Name and Email are required.

5. Неіснуючі сторінки

    URL: Будь-який інший

    Метод: GET/POST

    Опис: Повертає сторінку помилки 404 (404.html).

⚠️ Обмеження реалізації

Оскільки сервер написаний для навчальних цілей на базовому Node.js, він має певні технічні обмеження:

    Максимальний розмір POST-запиту: Сервер приймає тіло запиту розміром не більше 1 МБ (1 * 1024 * 1024 байт). Якщо запит перевищує цей ліміт, сервер розриває з'єднання і повертає помилку 413 Payload Too Large.

    Формат даних: Сервер налаштований на обробку даних лише у форматі application/x-www-form-urlencoded (стандартні HTML форми). Він не парсить JSON (application/json) автоматично.

    Шаблонізатор: Реалізовано примітивну систему шаблонів. Вона замінює лише прості змінні у форматі {{key}} і не підтримує складну логіку (цикли, умови) всередині HTML.

    Безпека (Sanitization): Функція sanitize виконує базове екранування символу < (замінює на &lt;) для захисту від простих XSS-атак.
